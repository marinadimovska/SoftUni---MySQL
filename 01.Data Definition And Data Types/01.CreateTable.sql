CREATE DATABASE `minions`;
USE `minions`;

CREATE TABLE `minions` (
`id` INT PRIMARY KEY AUTO_INCREMENT,
`name` VARCHAR(30),
`age` int
);

CREATE TABLE `town`(
`id` INT PRIMARY KEY AUTO_INCREMENT,
`name` VARCHAR(30)
);
